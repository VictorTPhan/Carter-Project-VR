using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PipeType : MonoBehaviour
{
    public enum Type {pipe, Lpipe, Tpipe};

    public Type pipeType;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
